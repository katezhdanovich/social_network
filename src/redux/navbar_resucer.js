let initialeState = {
    data_friends: [
        {id: 1, name: "Иван"},
        {id: 2, name: "Иван"},
        {id: 3, name: "Иван"},
    ]
}

const navbarReducer = (state = initialeState, action) => {
    return state
}

export default navbarReducer